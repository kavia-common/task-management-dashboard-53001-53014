# Database (provided by platform)
POSTGRES_URL=postgresql://localhost:5000/myapp

# Auth (MUST be set in the real .env by orchestrator/user)
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
